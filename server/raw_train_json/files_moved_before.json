[
  "files moved before {val_str}",
  "show files moved earlier than {val_str}",
  "which files were moved before {val_str}",
  "list all items moved before {val_str}",
  "moved prior to {val_str}",
  "files with move date before {val_str}",
  "get everything that was moved before {val_str}",
  "items moved earlier than {val_str}",
  "moved any time before {val_str}",
  "files last moved before {val_str}",
  "get files with move timestamp before {val_str}",
  "data moved earlier than {val_str}",
  "moved on a date less than {val_str}",
  "files whose move date precedes {val_str}",
  "show data moved before {val_str}",
  "retrieved moved files before {val_str}",
  "filter items moved before {val_str}",
  "everything shifted before {val_str}",
  "files whose movement happened before {val_str}",
  "show moved files older than {val_str}",
  "get items that were moved before {val_str}",
  "move date is before {val_str}",
  "last moved earlier than {val_str}",
  "what got moved before {val_str}",
  "was this moved before {val_str}?",
  "list moved entities before {val_str}",
  "files transferred before {val_str}",
  "shifted before {val_str}",
  "find everything that moved before {val_str}",
  "records of moves before {val_str}",
  "movement occurred before {val_str}",
  "files that changed location before {val_str}",
  "relocated prior to {val_str}",
  "moved on or before {val_str}",
  "documents moved earlier than {val_str}",
  "get last move before {val_str}",
  "any file moved before {val_str}",
  "find files moved ahead of {val_str}",
  "what was moved before {val_str}",
  "look up files moved before {val_str}",
  "track move events before {val_str}",
  "show all data that moved before {val_str}",
  "identify files moved before {val_str}",
  "files moved sometime before {val_str}",
  "moved in the past before {val_str}",
  "logs showing move before {val_str}",
  "movement date earlier than {val_str}",
  "get moved items prior to {val_str}",
  "all movements before {val_str}",
  "file transfers before {val_str}",
  "records with move date before {val_str}",
  "any file relocation before {val_str}",
  "check if moved before {val_str}",
  "grab files moved before {val_str}",
  "results of files moved before {val_str}",
  "data moved long before {val_str}",
  "get all moves before {val_str}",
  "moved timestamp is less than {val_str}",
  "file move logged before {val_str}",
  "items moved pre-{val_str}",
  "show me all movement before {val_str}",
  "show items whose move date is before {val_str}",
  "last known move before {val_str}",
  "was moved ahead of {val_str}",
  "any relocation earlier than {val_str}",
  "fetch movement logs before {val_str}",
  "sort by move date before {val_str}",
  "file action 'moved' happened before {val_str}",
  "check movement timeline before {val_str}",
  "logs where move date is before {val_str}",
  "files that had a move event before {val_str}",
  "detect movement before {val_str}",
  "scan for move activity before {val_str}",
  "filter files moved ahead of {val_str}",
  "moved before the date {val_str}",
  "location change happened before {val_str}",
  "relocation timestamp is before {val_str}",
  "transfer occurred before {val_str}",
  "migration event before {val_str}",
  "get historical moves before {val_str}",
  "show archive of moves before {val_str}",
  "file operations of type 'move' before {val_str}",
  "movement history before {val_str}",
  "query for moves before {val_str}",
  "movement happened earlier than {val_str}",
  "older movement than {val_str}",
  "fetch by move timestamp before {val_str}",
  "look for moved records before {val_str}",
  "past moves before {val_str}",
  "what records show move before {val_str}",
  "files moved to new location before {val_str}",
  "movement detected before {val_str}",
  "moved state changed before {val_str}",
  "list moved documents older than {val_str}",
  "verify move occurred before {val_str}",
  "check for moved status prior to {val_str}",
  "moved property timestamp before {val_str}",
  "select all files moved before {val_str}",
  "anything that relocated before {val_str}",
  "fetch entries where move < {val_str}",
  "pull moved entries from before {val_str}",
  "moved data earlier than {val_str}",
  "relocation action before {val_str}"
]
