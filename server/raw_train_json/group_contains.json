[
  "get me files where the group name contains {str}",
  "fetch all entries from groups with names containing {str}",
  "show me files whose group includes {str}",
  "list files where the group name has {str} in it",
  "return items grouped under names that contain {str}",
  "retrieve files with group names that include {str}",
  "find documents where the group name contains {str}",
  "search for files grouped under names with {str}",
  "I need files from groups with {str} in the name",
  "can you get me files where the group includes {str}",
  "filter items with group names containing {str}",
  "pull up files in groups that include {str}",
  "display entries grouped by names containing {str}",
  "locate documents from groups where name contains {str}",
  "identify files in groups with names that include {str}",
  "give me a list of files under groups with {str} in the name",
  "get everything from groups that have {str} in the name",
  "find records with group names that contain {str}",
  "search through entries grouped under names including {str}",
  "retrieve documents from groups containing {str}",
  "list all files from groups whose names contain {str}",
  "show me logs from group names including {str}",
  "list entries grouped under names that contain {str}",
  "fetch files with group names that include the string {str}",
  "load files from groups with names that have {str}",
  "highlight files from groups containing {str}",
  "look for entries with group names that include {str}",
  "extract data from groups with {str} somewhere in the name",
  "return just the files from groups containing {str}",
  "enumerate files grouped under names including {str}",
  "track down files from groups with names that contain {str}",
  "show group listings where the name includes {str}",
  "pull every item from groups with {str} in the name",
  "compile files grouped under names containing {str}",
  "let me view files grouped by names with {str}",
  "isolate records with group names including {str}",
  "note the group names that contain {str}",
  "locate documents under groups with {str} in the name",
  "trace records grouped with names including {str}",
  "grab files where group name contains {str}",
  "view entries from groups that include {str}",
  "access files with group names containing {str}",
  "list grouped records with names that include {str}",
  "report all items from groups with {str} in the name",
  "scan entries from group names containing {str}",
  "call up documents from groups with {str} inside the name",
  "open data from groups whose names contain {str}",
  "bring in items from groups that contain {str}",
  "get results from group names with {str}",
  "gather files under group names that include {str}",
  "let me see items from groups with {str} present in the name",
  "search group names that have {str} in them",
  "view dataset entries grouped under names containing {str}",
  "dig up files from groups with names that include {str}",
  "load records grouped by names that contain {str}",
  "point to items in group names with {str}",
  "summon files grouped under names with {str}",
  "render visible entries from groups including {str}",
  "audit logs grouped by names that contain {str}",
  "inspect documents from groups with {str} in the name",
  "bring together files under group names including {str}",
  "return content from group names that contain {str}",
  "review documents grouped under names with {str}",
  "check group names that include {str}",
  "filter group field for entries including {str}",
  "search logs from group names containing {str}",
  "lookup entries where group name includes {str}",
  "display files under group names that contain {str}",
  "fetch logs from group names with {str} in them",
  "scan for records from groups containing {str}",
  "track entries under group names that contain {str}",
  "access group fields that include {str}",
  "locate group identifiers that have {str} in them",
  "note down files from groups with names that include {str}",
  "bring online documents grouped under names that contain {str}",
  "trace group names containing {str}",
  "compile logs under groups with {str} present",
  "scan archives for group names including {str}",
  "present files under group names with {str} in them",
  "filter group list for entries containing {str}",
  "record items from group names that include {str}",
  "log files from groups whose names contain {str}",
  "detect entries in group names with {str}",
  "summarize entries grouped by names containing {str}",
  "generate list of groups that include {str}",
  "document entries where group name has {str}",
  "recover documents from groups with names including {str}",
  "match entries under group name including {str}",
  "organize group-based entries with names that contain {str}",
  "map records grouped under names with {str}",
  "index files grouped under names containing {str}",
  "report files from group names that include {str}",
  "review grouped data where the name has {str}",
  "log all records from groups containing {str}",
  "uncover entries in groups with names including {str}",
  "recall files grouped under names that contain {str}",
  "reveal content under groups with {str} in name",
  "compile reports for group names containing {str}",
  "filter group-related results for names including {str}",
  "scan filesystem for group names that contain {str}",
  "note items under group names with {str}",
  "explore records grouped under names containing {str}"
]
