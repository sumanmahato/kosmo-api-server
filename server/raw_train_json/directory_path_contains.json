[
  "get me directory paths that contain {str}",
  "fetch directory paths including {str}",
  "show me directory paths where the path contains {str}",
  "list directory paths which include {str}",
  "return directory paths with {str} in them",
  "retrieve directory paths containing {str}",
  "find directory paths that have {str} in them",
  "search for directory paths featuring {str}",
  "pull up directory paths whose path contains {str}",
  "display directory paths including {str}",
  "locate directory paths with substring {str}",
  "identify all directory paths that contain {str}",
  "give me a list of directory paths with {str}",
  "highlight every directory path that includes {str}",
  "look for directory paths that feature {str}",
  "extract directory paths containing {str}",
  "enumerate paths which contain {str}",
  "track down directory paths with {str} in the name",
  "compile list of all directory paths including {str}",
  "let me view paths that contain {str}",
  "isolate directory paths with embedded {str}",
  "note all directory paths where the path includes {str}",
  "trace directory paths containing {str}",
  "view directory paths where the path has {str}",
  "access entries with path containing {str}",
  "report directory paths that contain {str}",
  "call up every directory path with {str}",
  "open directory paths with {str} in them",
  "bring in all directory paths that contain {str}",
  "get results for paths including {str}",
  "gather directory paths with {str}",
  "let me see directory paths that include {str}",
  "dig up directory paths with substring {str}",
  "load all directory paths containing {str}",
  "point to directory paths that include {str}",
  "summon paths where path includes {str}",
  "render visible directory paths that contain {str}",
  "audit directory structure with {str} in path",
  "inspect directory paths including {str}",
  "bring together all paths with {str}",
  "review directory paths that include {str}",
  "check all directory paths that contain {str}",
  "filter for directory paths containing {str}",
  "lookup entries with path having {str}",
  "scan for paths that contain {str}",
  "match all directory paths featuring {str}",
  "organize directory listings with {str} in path",
  "map out paths with {str} in them",
  "index directory paths containing {str}",
  "record all paths with {str}",
  "log directory paths that include {str}",
  "uncover paths where directory contains {str}",
  "recall all directory paths with {str}",
  "reveal paths that contain {str}",
  "compile report of paths containing {str}",
  "note down all paths with {str}",
  "explore directory tree for {str}",
  "scan all directories whose path has {str}",
  "generate list of directory paths with {str} in them",
  "detect paths containing the text {str}",
  "isolate results that include {str} in path",
  "flag directory paths containing {str}",
  "read directory data where path includes {str}",
  "trace file tree with segments including {str}",
  "navigate paths containing {str}",
  "cross-check all directory paths with {str}",
  "visualize directory paths where path contains {str}",
  "verify paths that feature {str}",
  "return every directory path with {str}",
  "see all entries where path has {str}",
  "capture all directories whose path includes {str}",
  "enumerate structure with {str} in it",
  "sweep the directory paths for {str}",
  "flag all directories that contain {str}",
  "detect structure with path text {str}",
  "note all paths which include {str}",
  "lookup structure that contains {str}",
  "identify locations with path holding {str}",
  "observe entries that include {str}",
  "spot paths with segment {str}",
  "tag all directory paths that contain {str}",
  "summarize directories that contain {str}",
  "extract entries with {str} embedded",
  "list out all directory paths with {str}",
  "compile index of paths with {str} present",
  "query for directory listings that contain {str}",
  "structure search for paths having {str}",
  "trace every occurrence of {str} in directory paths",
  "match against directory paths containing {str}",
  "run a query for directory paths including {str}",
  "track items where path has {str}",
  "record paths where {str} is found",
  "check the directory listings containing {str}",
  "locate all directory names that contain {str}",
  "scan recursively for paths with {str}",
  "locate records with {str} in the path",
  "bring results that contain {str} in directory path",
  "file lookup for directories that contain {str}",
  "evaluate structure for entries including {str}",
  "track paths down that feature {str}",
  "verify directory entries containing {str}",
  "collect data from directories that include {str}",
  "inspect tree nodes for {str} in path"
]
