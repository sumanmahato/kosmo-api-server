[
  "get me files where the group name ends with {str}",
  "fetch all records from groups whose names end in {str}",
  "show me files with group names ending in {str}",
  "list files grouped under names that end with {str}",
  "return entries with group names that end in {str}",
  "retrieve data where the group name ends in {str}",
  "find documents in groups ending with {str}",
  "search for items where the group ends in {str}",
  "I need files from groups ending in {str}",
  "can you get me files where the group ends with {str}",
  "filter files whose group name ends with {str}",
  "pull up entries grouped under names ending in {str}",
  "display files from group names that end with {str}",
  "locate data from groups that end with {str}",
  "identify records under group names ending in {str}",
  "give me a list of items from groups that end with {str}",
  "get everything where the group ends in {str}",
  "find me all records with group names ending in {str}",
  "search through files from groups that end with {str}",
  "retrieve documents where the group name ends with {str}",
  "list out the data from groups ending in {str}",
  "show me logs grouped by names ending in {str}",
  "list all group entries that end with {str}",
  "fetch data from groups with names that end in {str}",
  "load files from group names ending with {str}",
  "highlight entries in groups that end with {str}",
  "look for files grouped under names ending in {str}",
  "extract documents from groups that end with {str}",
  "return just the entries from groups that end with {str}",
  "enumerate files grouped under names ending in {str}",
  "track records from groups ending in {str}",
  "show group listings with names ending in {str}",
  "pull every item from groups that end in {str}",
  "compile all files under group names ending in {str}",
  "let me view files from groups that end in {str}",
  "isolate data grouped under names ending in {str}",
  "note the entries from groups ending in {str}",
  "locate items grouped under names ending in {str}",
  "trace all files from groups that end with {str}",
  "grab records under group names ending in {str}",
  "view data from groups with names ending in {str}",
  "access records grouped by names that end in {str}",
  "list group items where name ends in {str}",
  "report on documents from groups ending in {str}",
  "scan entries in groups where name ends with {str}",
  "call up group records ending with {str}",
  "open files from groups ending in {str}",
  "bring in items from group names ending in {str}",
  "get matching groups where names end in {str}",
  "gather records from groups with names ending in {str}",
  "let me see everything grouped under names ending in {str}",
  "search group names ending in {str}",
  "view dataset entries with group suffix {str}",
  "dig up records grouped under names ending in {str}",
  "load entries from groups ending in {str}",
  "point to group names that end in {str}",
  "summon documents from groups ending in {str}",
  "render visible all items grouped under names ending in {str}",
  "audit records with group names ending in {str}",
  "inspect all files where the group ends in {str}",
  "bring together entries from groups ending in {str}",
  "return content from group names that end with {str}",
  "review data from groups whose names end in {str}",
  "check for group name endings matching {str}",
  "filter entries where the group ends with {str}",
  "search across logs grouped under names ending in {str}",
  "lookup documents with group names ending in {str}",
  "display files from groups with names ending in {str}",
  "fetch logs where group name ends in {str}",
  "scan for data grouped by names ending in {str}",
  "track all records under group names ending in {str}",
  "access logs with group names ending in {str}",
  "locate all records from groups that end in {str}",
  "note down entries with group names ending in {str}",
  "bring online files from groups that end in {str}",
  "trace datasets where group name ends with {str}",
  "compile reports from groups ending in {str}",
  "scan archives with group names ending in {str}",
  "present records where group ends in {str}",
  "filter dataset where group name ends with {str}",
  "record all entries from groups ending in {str}",
  "log records where group names end in {str}",
  "detect group names ending with {str}",
  "summarize groups that end with {str}",
  "generate group listings ending in {str}",
  "document data from group names ending in {str}",
  "recover documents from groups that end in {str}",
  "match files grouped by names ending in {str}",
  "organize entries under groups ending in {str}",
  "map records from group names ending in {str}",
  "index files with group names ending in {str}",
  "report on files from groups ending in {str}",
  "review contents from groups with names ending in {str}",
  "log group data that ends in {str}",
  "uncover group records ending in {str}",
  "recall entries from groups with names ending in {str}",
  "reveal all documents grouped by names ending in {str}",
  "compile audit from groups with names ending in {str}",
  "filter results from groups that end in {str}",
  "scan filesystem for group names ending in {str}",
  "note documents where group ends in {str}",
  "explore files from groups ending in {str}"
]
