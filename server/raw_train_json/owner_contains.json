[
  "get me files owned by users whose name contains {str}",
  "fetch all items where the owner's name includes {str}",
  "show me files with owner names containing {str}",
  "list every file where the owner field includes {str}",
  "return all files where the owner's name has {str} in it",
  "retrieve entries where the owner contains {str}",
  "find all files whose owner name includes {str}",
  "search for files with owner names that contain {str}",
  "I need files owned by someone whose name has {str}",
  "can you get me records where owner includes {str}",
  "filter items by owner names that contain {str}",
  "pull up all files where owner's name contains {str}",
  "display entries where owner name includes {str}",
  "locate all files with owner fields containing {str}",
  "identify files with owners whose names contain {str}",
  "give me a list of files with owner name including {str}",
  "get everything where the owner field contains {str}",
  "find me documents with owner names that have {str} in them",
  "search through files with owners containing {str}",
  "retrieve documents where owner name contains {str}",
  "list out all records with owner names that include {str}",
  "show me logs where the owner name contains {str}",
  "list entries that contain {str} in the owner's name",
  "fetch resources with owners that include {str}",
  "load files where the owner contains {str}",
  "highlight entries where the owner includes {str}",
  "look for files with owner names that include {str}",
  "extract all documents with {str} in the owner name",
  "return just files whose owners contain {str}",
  "enumerate all entries with owners including {str}",
  "track down files where owner's name has {str}",
  "show me directory contents where owner has {str}",
  "pull every file with owner field that includes {str}",
  "compile list of files with owner name containing {str}",
  "let me view records where owner's name includes {str}",
  "isolate files with owner fields containing {str}",
  "note the entries where owner includes {str}",
  "locate files where the owner name includes {str}",
  "trace all files with {str} in the owner's name",
  "grab documents owned by users whose names contain {str}",
  "view files where the owner's name has {str}",
  "access entries with {str} present in the owner field",
  "list logs owned by users whose names contain {str}",
  "report files with owner fields that include {str}",
  "scan files where owner includes the string {str}",
  "call up records where owner's name contains {str}",
  "open documents whose owner name includes {str}",
  "bring in files where the owner field contains {str}",
  "get results for files with owner name including {str}",
  "gather documents with owners containing {str}",
  "let me see all files with owners that include {str}",
  "search for files where owner field contains {str}",
  "view entries where owner name includes {str}",
  "dig up files with owner field that includes {str}",
  "load records where owner's name contains {str}",
  "point to files with owner field matching part {str}",
  "summon files where owner field has {str}",
  "render visible entries where owner includes {str}",
  "audit files with owner names that contain {str}",
  "inspect records with owner names containing {str}",
  "bring together files where owner name includes {str}",
  "return all entries for owners that include {str}",
  "review files with owner field containing {str}",
  "check files where owner's name includes {str}",
  "filter records for owner names that include {str}",
  "search across items with {str} in owner",
  "lookup documents with owner field containing {str}",
  "display logs where owner name includes {str}",
  "fetch entries where owner string contains {str}",
  "scan filesystems for owner name that includes {str}",
  "track ownership where name includes {str}",
  "access files belonging to users with {str} in their name",
  "locate users whose names contain {str}",
  "note down all items with owners containing {str}",
  "bring online resources with owner including {str}",
  "trace ownership for records with owner field having {str}",
  "compile list of entries with {str} in owner",
  "scan archives for owners containing {str}",
  "present items where the owner includes {str}",
  "filter files whose owner name includes {str}",
  "record entries with owner field containing {str}",
  "log documents owned by names that include {str}",
  "detect owners whose names contain {str}",
  "summarize ownership details containing {str}",
  "generate index of files with owner including {str}",
  "document entries with owner field containing {str}",
  "recover items where owner name contains {str}",
  "match records with owner field including {str}",
  "organize files with owner field containing {str}",
  "map items where owner field includes {str}",
  "index documents with owner name including {str}",
  "report on files with owners that contain {str}",
  "review structures with owner names that include {str}",
  "log ownership where names include {str}",
  "uncover entries with {str} in owner field",
  "recall documents with owner names containing {str}",
  "reveal owners with names that include {str}",
  "compile audit where owner contains {str}",
  "filter results with owners that include {str}",
  "scan records for users with names containing {str}",
  "note files with owners that include {str}",
  "explore documents with {str} in owner field"
]
