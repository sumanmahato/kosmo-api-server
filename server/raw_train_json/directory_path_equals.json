[
  "get me directory paths exactly equal to {str}",
  "fetch all directory paths where the path is {str}",
  "show me directory paths that equal {str}",
  "list every directory path matching {str}",
  "return all directories whose path equals {str}",
  "retrieve paths that exactly match {str}",
  "find all directory paths that are equal to {str}",
  "search for directory paths equal to {str}",
  "I need directory paths that match exactly {str}",
  "can you get me paths that are {str}",
  "filter directory paths where the value is {str}",
  "pull up paths that equal {str}",
  "display directory paths matching {str}",
  "locate all paths with value {str}",
  "identify paths equal to {str}",
  "give me a list of directory paths that are {str}",
  "get everything where path is exactly {str}",
  "find me directories with path {str}",
  "search through directory paths where path equals {str}",
  "retrieve directories that match {str}",
  "list out the paths that are equal to {str}",
  "show me all directory paths equal to {str}",
  "list all entries whose path is {str}",
  "fetch directory paths that are exactly {str}",
  "load all directories where path equals {str}",
  "highlight paths matching exactly {str}",
  "look for paths that equal {str}",
  "extract all paths equal to {str}",
  "return just the paths that match {str}",
  "enumerate all directories whose path is {str}",
  "track down all directory paths that are {str}",
  "show me directory listings that exactly match {str}",
  "pull every directory with exact path {str}",
  "compile list of directory paths equal to {str}",
  "let me view all paths matching {str}",
  "isolate directory paths with exact value {str}",
  "note the paths that equal {str}",
  "locate all directory paths that match exactly {str}",
  "trace the paths where path equals {str}",
  "grab every directory path equal to {str}",
  "view directory tree where path is {str}",
  "access the paths equal to {str}",
  "list path entries that are {str}",
  "report all directory paths that match {str}",
  "scan paths that equal {str}",
  "call up directory paths that are {str}",
  "open all paths equal to {str}",
  "bring in directory paths with value {str}",
  "get results for paths that equal {str}",
  "gather all directory paths where path equals {str}",
  "let me see everything where path is {str}",
  "search for directories that match exactly {str}",
  "view paths in directory tree that are {str}",
  "dig up paths equal to {str}",
  "load resources whose path is {str}",
  "point to directory paths equal to {str}",
  "summon all paths matching {str}",
  "render visible all paths equal to {str}",
  "audit directory tree for path equal to {str}",
  "inspect all directory paths whose value is {str}",
  "bring together directory paths matching {str}",
  "return all content from paths equal to {str}",
  "review paths that match {str}",
  "check directory tree where path equals {str}",
  "filter path entries where path equals {str}",
  "search paths whose path is {str}",
  "lookup directories with exact path {str}",
  "display tree paths equal to {str}",
  "fetch records for directories equal to {str}",
  "scan for paths exactly equal to {str}",
  "track all directories whose path is {str}",
  "access structure where path is exactly {str}",
  "locate root paths equal to {str}",
  "note down path entries matching {str}",
  "bring online directories with path {str}",
  "trace path entries where path equals {str}",
  "compile paths exactly matching {str}",
  "scan archive for directory paths equal to {str}",
  "present paths whose path is {str}",
  "filter filesystem where path equals {str}",
  "record directories with path {str}",
  "log all directory path entries equal to {str}",
  "detect directories where path matches {str}",
  "summarize entries that match exactly {str}",
  "generate list of pathnames equal to {str}",
  "document tree nodes with value {str}",
  "recover path entries that are {str}",
  "match directory entries whose path is {str}",
  "organize paths equal to {str}",
  "map out directories that equal {str}",
  "index all directory paths matching {str}",
  "report on paths whose path is {str}",
  "review folder structure matching {str}",
  "log all directory paths where path is {str}",
  "uncover all directories with path equal to {str}",
  "recall paths that equal {str}",
  "reveal directories whose path is {str}",
  "compile report of paths equal to {str}",
  "filter results for directories matching {str}",
  "scan filesystem for paths equal to {str}",
  "note the directories where path is {str}",
  "explore all paths that match {str}"
]
