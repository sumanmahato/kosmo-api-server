[
  "get me directory paths that end with {str}",
  "fetch all directory paths ending with {str}",
  "show me directory paths where the path ends in {str}",
  "list every directory path that ends with {str}",
  "return all directories whose path ends with {str}",
  "retrieve paths that finish with {str}",
  "find all directory paths with suffix {str}",
  "search for directory paths that end in {str}",
  "I need directory paths ending in {str}",
  "can you get me paths that end with {str}",
  "filter directory paths with ending segment {str}",
  "pull up paths that end with {str}",
  "display directory paths finishing with {str}",
  "locate all paths that end in {str}",
  "identify paths ending with {str}",
  "give me a list of directory paths that end with {str}",
  "get everything where path concludes with {str}",
  "find me directories whose path ends with {str}",
  "search through directory paths ending in {str}",
  "retrieve directories that have suffix {str}",
  "list out the paths that end in {str}",
  "show me all directory paths that end with {str}",
  "list all entries where path ends with {str}",
  "fetch directory paths where the last segment is {str}",
  "load all directories ending with {str}",
  "highlight paths that end with {str}",
  "look for paths with suffix {str}",
  "extract all paths finishing with {str}",
  "return just the paths that end in {str}",
  "enumerate all directories that end with {str}",
  "track down all directory paths ending in {str}",
  "show me directory listings that end with {str}",
  "pull every directory with path ending in {str}",
  "compile list of directory paths ending in {str}",
  "let me view all paths that end with {str}",
  "isolate directory paths that finish with {str}",
  "note the paths that end with {str}",
  "locate all directory paths with ending {str}",
  "trace the paths that end in {str}",
  "grab every directory path that ends with {str}",
  "view directory tree ending with {str}",
  "access the paths whose last part is {str}",
  "list path entries ending with {str}",
  "report all directory paths that end in {str}",
  "scan paths ending with {str}",
  "call up directory paths finishing in {str}",
  "open all paths that conclude with {str}",
  "bring in directory paths that end in {str}",
  "get results for paths ending in {str}",
  "gather all directory paths ending in {str}",
  "let me see everything with path ending in {str}",
  "search for directories that finish with {str}",
  "view paths in directory tree ending with {str}",
  "dig up paths where the last part is {str}",
  "load resources that end with {str}",
  "point to directory paths ending in {str}",
  "summon all paths whose suffix is {str}",
  "render visible all paths that end in {str}",
  "audit directory tree for paths ending with {str}",
  "inspect all directory paths that end with {str}",
  "bring together directory paths ending in {str}",
  "return all content from paths that end in {str}",
  "review paths with suffix {str}",
  "check directory tree where path ends with {str}",
  "filter path entries ending in {str}",
  "search paths that end in {str}",
  "lookup directories ending in {str}",
  "display tree paths ending with {str}",
  "fetch records for directories with ending {str}",
  "scan for paths with suffix {str}",
  "track all directories whose path ends with {str}",
  "access structure ending with {str}",
  "locate root paths that end with {str}",
  "note down path entries with last part {str}",
  "bring online directories that end with {str}",
  "trace path entries that end with {str}",
  "compile paths that end in {str}",
  "scan archive for directory paths that end in {str}",
  "present paths ending with {str}",
  "filter filesystem for paths ending in {str}",
  "record directories ending with {str}",
  "log all directory path entries with suffix {str}",
  "detect directories that end with {str}",
  "summarize entries that end in {str}",
  "generate list of pathnames with ending {str}",
  "document tree nodes ending with {str}",
  "recover path entries that end with {str}",
  "match directory entries ending in {str}",
  "organize paths that end with {str}",
  "map out directories ending in {str}",
  "index all directory paths with suffix {str}",
  "report on paths ending with {str}",
  "review folder structure with ending {str}",
  "log all directory paths with final part {str}",
  "uncover all directories ending in {str}",
  "recall paths that end in {str}",
  "reveal directories that finish with {str}",
  "compile report of paths ending in {str}",
  "filter results for directories that end with {str}",
  "scan filesystem for directory paths ending in {str}",
  "note the directories that end with {str}",
  "explore all paths that end with {str}"
]
