[
  "get me directory paths that end with {str}",
  "get me directory paths ending with {str}",
  "get me directory paths which end in {str}",
  "get me directory paths that finish with {str}",
  "get me directory paths with suffix {str}",
  "get me directory paths concluding with {str}",
  "get me directory paths where the path ends with {str}",
  "get me directory paths whose path ends in {str}",
  "get me directory paths terminating in {str}",
  "get me directory paths that have {str} at the end",
  "fetch directory paths that end with {str}",
  "fetch directory paths ending with {str}",
  "fetch directory paths which end in {str}",
  "fetch directory paths that finish with {str}",
  "fetch directory paths with suffix {str}",
  "fetch directory paths concluding with {str}",
  "fetch directory paths where the path ends with {str}",
  "fetch directory paths whose path ends in {str}",
  "fetch directory paths terminating in {str}",
  "fetch directory paths that have {str} at the end",
  "list directory paths that end with {str}",
  "list directory paths ending with {str}",
  "list directory paths which end in {str}",
  "list directory paths that finish with {str}",
  "list directory paths with suffix {str}",
  "list directory paths concluding with {str}",
  "list directory paths where the path ends with {str}",
  "list directory paths whose path ends in {str}",
  "list directory paths terminating in {str}",
  "list directory paths that have {str} at the end",
  "show me directory paths that end with {str}",
  "show me directory paths ending with {str}",
  "show me directory paths which end in {str}",
  "show me directory paths that finish with {str}",
  "show me directory paths with suffix {str}",
  "show me directory paths concluding with {str}",
  "show me directory paths where the path ends with {str}",
  "show me directory paths whose path ends in {str}",
  "show me directory paths terminating in {str}",
  "show me directory paths that have {str} at the end",
  "return directory paths that end with {str}",
  "return directory paths ending with {str}",
  "return directory paths which end in {str}",
  "return directory paths that finish with {str}",
  "return directory paths with suffix {str}",
  "return directory paths concluding with {str}",
  "return directory paths where the path ends with {str}",
  "return directory paths whose path ends in {str}",
  "return directory paths terminating in {str}",
  "return directory paths that have {str} at the end",
  "retrieve directory paths that end with {str}",
  "retrieve directory paths ending with {str}",
  "retrieve directory paths which end in {str}",
  "retrieve directory paths that finish with {str}",
  "retrieve directory paths with suffix {str}",
  "retrieve directory paths concluding with {str}",
  "retrieve directory paths where the path ends with {str}",
  "retrieve directory paths whose path ends in {str}",
  "retrieve directory paths terminating in {str}",
  "retrieve directory paths that have {str} at the end",
  "find directory paths that end with {str}",
  "find directory paths ending with {str}",
  "find directory paths which end in {str}",
  "find directory paths that finish with {str}",
  "find directory paths with suffix {str}",
  "find directory paths concluding with {str}",
  "find directory paths where the path ends with {str}",
  "find directory paths whose path ends in {str}",
  "find directory paths terminating in {str}",
  "find directory paths that have {str} at the end",
  "search for directory paths that end with {str}",
  "search for directory paths ending with {str}",
  "search for directory paths which end in {str}",
  "search for directory paths that finish with {str}",
  "search for directory paths with suffix {str}",
  "search for directory paths concluding with {str}",
  "search for directory paths where the path ends with {str}",
  "search for directory paths whose path ends in {str}",
  "search for directory paths terminating in {str}",
  "search for directory paths that have {str} at the end",
  "pull up directory paths that end with {str}",
  "pull up directory paths ending with {str}",
  "pull up directory paths which end in {str}",
  "pull up directory paths that finish with {str}",
  "pull up directory paths with suffix {str}",
  "pull up directory paths concluding with {str}",
  "pull up directory paths where the path ends with {str}",
  "pull up directory paths whose path ends in {str}",
  "pull up directory paths terminating in {str}",
  "pull up directory paths that have {str} at the end",
  "display directory paths that end with {str}",
  "display directory paths ending with {str}",
  "display directory paths which end in {str}",
  "display directory paths that finish with {str}",
  "display directory paths with suffix {str}",
  "display directory paths concluding with {str}",
  "display directory paths where the path ends with {str}",
  "display directory paths whose path ends in {str}",
  "display directory paths terminating in {str}",
  "display directory paths that have {str} at the end"
]
