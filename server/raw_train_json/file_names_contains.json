[
  "get me files whose names contain {str}",
  "fetch all files that have {str} in their name",
  "show me all files containing {str}",
  "list every file that includes {str} in its name",
  "return all files with {str} somewhere in the name",
  "retrieve files where the filename includes {str}",
  "find all files with names that contain {str}",
  "search for files whose names contain {str}",
  "I need files with {str} in the name",
  "can you get me files that contain {str}",
  "filter files that have {str} in the filename",
  "pull up files that include {str}",
  "display files with {str} in the name",
  "locate all files containing {str}",
  "identify files that have {str} in their name",
  "give me a list of files with {str} in the name",
  "get everything that has {str} in its filename",
  "find me all documents containing {str}",
  "search through files with {str} in the name",
  "retrieve all documents with filenames that contain {str}",
  "list out the files that include {str}",
  "get me a list of all files containing {str}",
  "show me documents where the name contains {str}",
  "list all entries where name includes {str}",
  "fetch files that contain {str}",
  "load all files where name has {str}",
  "highlight files with names including {str}",
  "look for filenames that contain {str}",
  "extract all files with {str} in name",
  "return just the files that contain {str}",
  "enumerate all files where name includes {str}",
  "track down all files containing {str}",
  "show logs that contain {str}",
  "pull every file that has {str} in it",
  "compile list of files with {str}",
  "let me view files with {str} in their names",
  "isolate files named with {str}",
  "note the files that contain {str}",
  "locate files named with {str}",
  "trace files that include {str}",
  "grab everything that has {str} in the name",
  "view all documents that have {str}",
  "access the files with {str} in the filename",
  "list logs that contain {str}",
  "report all items with {str} in the name",
  "scan files whose names have {str}",
  "call up files with {str} in their name",
  "open all files that contain {str}",
  "bring in files that contain {str}",
  "get all results for files including {str}",
  "gather entries that contain {str}",
  "let me see everything that has {str} in the filename",
  "search logs that include {str}",
  "view dataset entries that have {str}",
  "dig up files with {str} in the name",
  "load resources where name contains {str}",
  "point to files named with {str}",
  "summon files with names containing {str}",
  "render visible all files that contain {str}",
  "audit filenames that have {str}",
  "inspect all files including {str}",
  "bring together files with {str} inside the name",
  "return content with {str} in the filename",
  "review all files that contain {str}",
  "check for files with {str} anywhere in the name",
  "filter dataset for name containing {str}",
  "search entries with {str} in name",
  "lookup documents that contain {str}",
  "display items where name has {str}",
  "fetch logs that contain {str}",
  "scan for file names containing {str}",
  "track all files that have {str}",
  "access logs with {str} inside the name",
  "locate records with {str} in the filename",
  "note down files that contain {str}",
  "bring online files with {str}",
  "trace items with name containing {str}",
  "compile dataset with {str} in filenames",
  "scan archive for entries that have {str}",
  "present all files that contain {str}",
  "filter everything by file name containing {str}",
  "record documents with {str} in the name",
  "log all items containing {str}",
  "detect files that include {str}",
  "summarize files that have {str}",
  "generate list of filenames containing {str}",
  "document all file entries with {str}",
  "recover files with {str} in their name",
  "list all matches for filenames with {str}",
  "organize records that contain {str}",
  "map all files including {str}",
  "index entries where name has {str}",
  "report documents with {str} in their name",
  "review content that contains {str}",
  "log files with {str} in the filename",
  "uncover files that contain {str}",
  "recall documents with names containing {str}",
  "reveal filenames with {str}",
  "compile report of files containing {str}",
  "filter results by name including {str}",
  "scan filesystem for names containing {str}",
  "note the items whose names contain {str}",
  "explore all files that have {str} in their name"
]
